rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // SHA-256 해시(64자리 hex)만 문서 ID로 허용
    // 아이디를 알아야만 읽기/쓰기 가능 (Firebase 인증 대신 해시 자체가 보안 키)
    match /users/{userId} {
      allow read, write: if userId.size() == 64;
    }
  }
}
